.DS_Store
__pycache__/
*.pyc

# Build products. Everything here is regenerated by scripts/install.py.
build/
macos-app/.build/

# Firmware images and VIA keymap dumps are per-machine, and the factory image
# is NuPhy's binary, not ours to redistribute. firmware/README.md says how to
# take your own with dfu-util -U before you flash anything.
backups/
